<?xml version="1.0" encoding="utf-8"?>
<!--
    PANTALLA DE CARGA (P1)
    Logo en círculo blanco, nombre UBICAFÉ, eslogan y ubicación al pie.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fondo_crema"
    android:orientation="vertical">

    <!-- Bloque central: logo + textos -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/logo"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:background="@drawable/fondo_circulo_blanco"
            android:contentDescription="@string/app_name"
            android:padding="23dp"
            android:src="@drawable/ic_logo_ubicafe" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="22dp"
            android:fontFamily="@font/nunito_extra_bold"
            android:text="@string/app_nombre_largo"
            android:textColor="@color/verde_oscuro"
            android:textSize="32sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:fontFamily="@font/nunito_sans_medium"
            android:text="@string/app_eslogan"
            android:textColor="@color/texto_secundario"
            android:textSize="17sp" />
    </LinearLayout>

    <!-- Pie: ubicación -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="36dp"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="14dp"
            android:layout_height="18dp"
            android:contentDescription="@null"
            android:src="@drawable/ic_map_pin"
            app:tint="@color/texto_tenue"
            xmlns:app="http://schemas.android.com/apk/res-auto" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:fontFamily="@font/nunito_sans_medium"
            android:text="@string/app_ubicacion"
            android:textColor="@color/texto_tenue"
            android:textSize="13sp" />
    </LinearLayout>
</LinearLayout>